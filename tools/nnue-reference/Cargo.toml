[package]
name = "neyrang-nnue-reference"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
publish = false
description = "Scalar correctness oracle for NEYRANG NNUE experiments"

[dependencies]
neyrang = { path = "../..", features = ["nnue"] }
neyrang-nnue-data = { path = "../nnue-data" }

[lints.rust]
unsafe_code = "forbid"
